# Framework wtyczek scraperów + 3 portale — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pluginowy framework scraperów (interfejs + rejestr) oraz działające, przetestowane parsery dla otodom.pl, nieruchomosci-online.pl i hossa.gda.pl, plus warstwa pobierania Playwright — wszystko testowane offline na realnych fixture'ach HTML.

**Architecture:** Każdy portal = wtyczka rozdzielona na (a) **czyste parsery** HTML→`RawListing` (deterministyczne, testowane na nagranych realnych stronach) i (b) **fetcher Playwright** (pobieranie + paginacja + rate-limit + wykrywanie blokady). Wspólny `Scraper` (Protocol) + rejestr wtyczek. Parsery nie dotykają sieci; fetcher nie parsuje — granica pozwala testować parsowanie bez przeglądarki, a pobieranie na lokalnym serwerze statycznym.

**Tech Stack:** Python 3.14, Playwright (chromium), selectolax (szybki parser HTML/CSS), Pydantic v2. Testy: pytest, lokalny `http.server` serwujący fixture'y do testów fetchera.

## Global Constraints

- Python 3.14; uruchamianie przez **uv** (`uv run ...`).
- Parsery są **czystymi funkcjami** (HTML/str → `list[RawListing]` / `RawListing`); NIE wykonują I/O.
- Testy parserów działają **offline** na nagranych fixture'ach (`tests/fixtures/`), bez sieci.
- Fixture'y HTML przechowywane **gzipowane** (`*.html.gz`) i ładowane przez `load_fixture()`.
- Żaden test w domyślnym przebiegu nie łączy się z prawdziwymi portalami. Testy „na żywo" oznaczone `@pytest.mark.live` i wykluczone domyślnie.
- `RawListing` to surowy wynik scrapingu; mapowanie na ORM `Listing` należy do Planu 3 (normalizator) — NIE rób tego tutaj.
- Lint `uv run ruff check .` musi przechodzić. TDD: czerwony test → implementacja → zielony → commit.
- Brak hardcodowanych sekretów; rate-limity/UA konfigurowalne (rozszerzą `Settings`).

## Konwencje danych (współdzielone)

`RawListing` (pydantic) — surowa oferta z dowolnego portalu:
- `source_id: str`, `external_id: str`, `url: str`, `title: str`
- `price: Decimal | None`, `area_m2: float | None`, `rooms: int | None`
- `floor: int | None`, `total_floors: int | None`
- `city: str | None`, `district: str | None`, `street: str | None`
- `market: str | None` ("primary"/"secondary"/None), `description: str | None`
- `images: list[str] = []`, `posted_at: datetime | None = None`
- `raw: dict = {}` (dowolne dodatkowe pola źródłowe)

`SearchCriteria` (pydantic) — wejście wyszukiwania:
- `city: str` (wymagane, np. "gdansk")
- `min_price: int | None`, `max_price: int | None`
- `min_area: float | None`, `max_area: float | None`
- `min_rooms: int | None`, `max_rooms: int | None`
- `market: str | None`

---

### Task 1: Fundament frameworku scraperów (modele, interfejs, rejestr, Playwright dep)

**Files:**
- Create: `src/realestate/scrapers/__init__.py`
- Create: `src/realestate/scrapers/base.py`
- Modify: `pyproject.toml` (dodaj `playwright`, `selectolax`)
- Test: `tests/scrapers/__init__.py`
- Test: `tests/scrapers/test_base.py`

**Interfaces:**
- Produces:
  - `class SearchCriteria(BaseModel)` (pola jak wyżej).
  - `class RawListing(BaseModel)` (pola jak wyżej).
  - `class ScraperBlocked(Exception)` — sygnał wykrytej blokady anty-bot.
  - `class Scraper(Protocol)`:
    - `source_id: str`, `display_name: str`
    - `build_search_url(self, criteria: SearchCriteria, page: int) -> str`
    - `parse_search(self, html: str) -> list[RawListing]`
    - `parse_detail(self, html: str, url: str) -> RawListing`
  - Rejestr: `register(scraper: Scraper) -> None`, `get_scrapers() -> dict[str, Scraper]`, `get_scraper(source_id: str) -> Scraper`. Rejestr to prosty słownik modułowy `_REGISTRY`.

- [ ] **Step 1: Dodaj zależności**

Run: `uv add playwright selectolax`
Expected: `pyproject.toml` zyskuje `playwright` i `selectolax`; `uv.lock` zaktualizowany. (Instalację przeglądarki chromium robi Task 6 — tu nie jest potrzebna.)

- [ ] **Step 2: Napisz failujący test**

`tests/scrapers/__init__.py`: (pusty)

`tests/scrapers/test_base.py`:
```python
import pytest

from realestate.scrapers.base import (
    RawListing,
    Scraper,
    ScraperBlocked,
    SearchCriteria,
    get_scraper,
    get_scrapers,
    register,
)


class _Dummy:
    source_id = "dummy"
    display_name = "Dummy"

    def build_search_url(self, criteria: SearchCriteria, page: int) -> str:
        return f"https://dummy/{criteria.city}?page={page}"

    def parse_search(self, html: str) -> list[RawListing]:
        return [RawListing(source_id="dummy", external_id="1", url="https://dummy/1", title="x")]

    def parse_detail(self, html: str, url: str) -> RawListing:
        return RawListing(source_id="dummy", external_id="1", url=url, title="x")


def test_raw_listing_defaults():
    rl = RawListing(source_id="s", external_id="e", url="u", title="t")
    assert rl.images == [] and rl.raw == {} and rl.price is None


def test_search_criteria_requires_city():
    with pytest.raises(Exception):
        SearchCriteria()
    assert SearchCriteria(city="gdansk").city == "gdansk"


def test_registry_register_and_get():
    d = _Dummy()
    register(d)
    assert get_scraper("dummy") is d
    assert "dummy" in get_scrapers()


def test_dummy_conforms_to_protocol():
    d = _Dummy()
    assert isinstance(d, Scraper)  # runtime_checkable Protocol


def test_scraper_blocked_is_exception():
    assert issubclass(ScraperBlocked, Exception)
```

- [ ] **Step 3: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_base.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'realestate.scrapers.base'`.

- [ ] **Step 4: Zaimplementuj `base.py`**

`src/realestate/scrapers/base.py`:
```python
from __future__ import annotations

from datetime import datetime
from decimal import Decimal
from typing import Protocol, runtime_checkable

from pydantic import BaseModel, Field


class SearchCriteria(BaseModel):
    city: str
    min_price: int | None = None
    max_price: int | None = None
    min_area: float | None = None
    max_area: float | None = None
    min_rooms: int | None = None
    max_rooms: int | None = None
    market: str | None = None


class RawListing(BaseModel):
    source_id: str
    external_id: str
    url: str
    title: str
    price: Decimal | None = None
    area_m2: float | None = None
    rooms: int | None = None
    floor: int | None = None
    total_floors: int | None = None
    city: str | None = None
    district: str | None = None
    street: str | None = None
    market: str | None = None
    description: str | None = None
    images: list[str] = Field(default_factory=list)
    posted_at: datetime | None = None
    raw: dict = Field(default_factory=dict)


class ScraperBlocked(Exception):
    """Wykryto blokadę anty-bot / wymóg captcha."""


@runtime_checkable
class Scraper(Protocol):
    source_id: str
    display_name: str

    def build_search_url(self, criteria: SearchCriteria, page: int) -> str: ...
    def parse_search(self, html: str) -> list[RawListing]: ...
    def parse_detail(self, html: str, url: str) -> RawListing: ...


_REGISTRY: dict[str, Scraper] = {}


def register(scraper: Scraper) -> None:
    _REGISTRY[scraper.source_id] = scraper


def get_scrapers() -> dict[str, Scraper]:
    return dict(_REGISTRY)


def get_scraper(source_id: str) -> Scraper:
    return _REGISTRY[source_id]
```

`src/realestate/scrapers/__init__.py`:
```python
from realestate.scrapers.base import (
    RawListing,
    Scraper,
    ScraperBlocked,
    SearchCriteria,
    get_scraper,
    get_scrapers,
    register,
)

__all__ = [
    "RawListing",
    "Scraper",
    "ScraperBlocked",
    "SearchCriteria",
    "get_scraper",
    "get_scrapers",
    "register",
]
```

- [ ] **Step 5: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_base.py -v`
Expected: PASS (5 testów).

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock src/realestate/scrapers/ tests/scrapers/
git commit -m "feat: fundament scraperów (RawListing, SearchCriteria, Scraper, rejestr)"
```

---

### Task 2: Infrastruktura fixture'ów (gzip realnych stron + loader)

**Files:**
- Create: `tests/fixtures/__init__.py`
- Create: `tests/fixtures/loader.py`
- Create (gzip z istniejących surowych przechwyceń): `tests/fixtures/data/otodom_search_gdansk.html.gz`, `tests/fixtures/data/nieruchomosci_online_search_gdansk.html.gz`, `tests/fixtures/data/hossa_home.html.gz`
- Delete (po gzipowaniu): `tests/fixtures/raw/*.html` (surowe, niespakowane)
- Test: `tests/fixtures/test_loader.py`

**Interfaces:**
- Consumes: surowe HTML w `tests/fixtures/raw/` (już przechwycone realne strony otodom/nieruchomosci-online/hossa).
- Produces: `load_fixture(name: str) -> str` — czyta `tests/fixtures/data/<name>.html.gz` i zwraca odkodowany tekst UTF-8.

- [ ] **Step 1: Spakuj surowe fixture'y i usuń niespakowane**

Run:
```bash
mkdir -p tests/fixtures/data
for f in otodom_search_gdansk nieruchomosci_online_search_gdansk hossa_home; do
  gzip -c "tests/fixtures/raw/$f.html" > "tests/fixtures/data/$f.html.gz"
done
git rm -r --cached tests/fixtures/raw 2>/dev/null || true
rm -rf tests/fixtures/raw
ls -la tests/fixtures/data
```
Expected: trzy pliki `*.html.gz` (łącznie znacznie mniej niż 2.6MB surowych).

- [ ] **Step 2: Napisz failujący test**

`tests/fixtures/__init__.py`: (pusty)

`tests/fixtures/test_loader.py`:
```python
from tests.fixtures.loader import load_fixture


def test_load_otodom_fixture():
    html = load_fixture("otodom_search_gdansk")
    assert "__NEXT_DATA__" in html
    assert len(html) > 100_000


def test_load_nieruchomosci_fixture():
    html = load_fixture("nieruchomosci_online_search_gdansk")
    assert len(html) > 100_000


def test_load_hossa_fixture():
    html = load_fixture("hossa_home")
    assert len(html) > 10_000
```

- [ ] **Step 3: Uruchom test — ma faliować**

Run: `uv run pytest tests/fixtures/test_loader.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'tests.fixtures.loader'`.

- [ ] **Step 4: Zaimplementuj loader**

`tests/fixtures/loader.py`:
```python
import gzip
from pathlib import Path

_DATA = Path(__file__).parent / "data"


def load_fixture(name: str) -> str:
    path = _DATA / f"{name}.html.gz"
    with gzip.open(path, "rt", encoding="utf-8", errors="replace") as fh:
        return fh.read()
```

- [ ] **Step 5: Uruchom test — ma przejść**

Run: `uv run pytest tests/fixtures/test_loader.py -v`
Expected: PASS (3 testy).

- [ ] **Step 6: Commit**

```bash
git add tests/fixtures/
git commit -m "test: fixture'y realnych stron (gzip) + loader"
```

---

### Task 3: Parser otodom.pl (z `__NEXT_DATA__`)

**Files:**
- Create: `src/realestate/scrapers/otodom.py`
- Test: `tests/scrapers/test_otodom_parser.py`

**Interfaces:**
- Consumes: `RawListing`, `SearchCriteria`, `Scraper`, `register`; fixture `otodom_search_gdansk`.
- Produces: `class OtodomScraper` z `source_id="otodom"`, `display_name="Otodom"`, implementujący `build_search_url`, `parse_search`, `parse_detail`. Instancja zarejestrowana przez `register(OtodomScraper())` na końcu modułu.
- otodom przechowuje dane wyszukiwania w `<script id="__NEXT_DATA__" type="application/json">` pod ścieżką `props.pageProps.data.searchAds.items`. Pola itemu (zweryfikowane): `id`, `title`, `slug`/`href`, `totalPrice`/`price`, `areaInSquareMeters`, `roomsNumber`, `floorNumber`, `locationLabel`/`location`, `dateCreated`, `images`/`photos`, `market`.

- [ ] **Step 1: Napisz failujący test (na realnym fixture)**

`tests/scrapers/test_otodom_parser.py`:
```python
from decimal import Decimal

from realestate.scrapers.base import SearchCriteria
from realestate.scrapers.otodom import OtodomScraper
from tests.fixtures.loader import load_fixture


def test_parse_search_returns_listings():
    html = load_fixture("otodom_search_gdansk")
    listings = OtodomScraper().parse_search(html)
    # fixture zawiera ~34 pozycje w searchAds.items
    assert len(listings) >= 20
    first = listings[0]
    assert first.source_id == "otodom"
    assert first.external_id  # niepuste
    assert first.url.startswith("https://www.otodom.pl/")
    assert first.title


def test_parse_search_extracts_numeric_fields():
    html = load_fixture("otodom_search_gdansk")
    listings = OtodomScraper().parse_search(html)
    priced = [x for x in listings if x.price is not None]
    assert priced, "co najmniej jedna oferta ma cenę"
    assert all(isinstance(x.price, Decimal) for x in priced)
    witharea = [x for x in listings if x.area_m2 is not None]
    assert witharea and all(x.area_m2 > 0 for x in witharea)


def test_build_search_url_contains_city():
    url = OtodomScraper().build_search_url(SearchCriteria(city="gdansk"), page=2)
    assert "gdansk" in url.lower()
    assert "page=2" in url or "/2" in url
```

- [ ] **Step 2: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_otodom_parser.py -v`
Expected: FAIL — brak modułu `realestate.scrapers.otodom`.

- [ ] **Step 3: Zaimplementuj `otodom.py`**

Wskazówki implementacyjne (parser ma być odporny — pola mogą być częściowo nieobecne):
- Wyłuskaj JSON regexem `r'<script[^>]*id="__NEXT_DATA__"[^>]*>(.*?)</script>'` (flaga DOTALL), `json.loads`.
- Przejdź do `data["props"]["pageProps"]["data"]["searchAds"]["items"]` (użyj `.get(...)` z fallbackami na `[]`).
- Dla każdego itemu zmapuj:
  - `external_id = str(item["id"])`
  - `url` = `"https://www.otodom.pl/pl/oferta/" + item["slug"]` jeśli jest `slug`, w przeciwnym razie z `item["href"]` (zbuduj absolutny URL).
  - `title = item.get("title")`
  - `price`: z `item["totalPrice"]["value"]` lub `item["price"]` jeśli liczba — `Decimal(str(...))`; przy `hidePrice` lub braku → `None`.
  - `area_m2 = item.get("areaInSquareMeters")`
  - `rooms`: `item.get("roomsNumber")` (czasem string typu "THREE" — wtedy `None`, zostaw mapowanie liczb tylko gdy int/da się rzutować).
  - `floor`: `item.get("floorNumber")` jeśli da się rzutować na int.
  - `market`: `item.get("market")` zmapuj na "primary"/"secondary" jeśli da się, inaczej `None`.
  - `images`: z `item.get("images")`/`item.get("photos")` wyłuskaj listę URLi (pola typu `large`/`medium`/`url`).
  - `district`: z `item["location"]` jeśli zawiera dzielnicę; inaczej `None`.
  - `raw = item` (pełny słownik źródłowy).
- `parse_detail(html, url)`: dla strony oferty otodom również jest `__NEXT_DATA__` pod `props.pageProps.ad`; wyłuskaj opis (`description`), `images`, `street`/`district` z `location`. Jeśli nie znajdziesz — zwróć `RawListing` z minimalnymi polami (`source_id`, `external_id` z URL, `url`, `title=""`). Implementuj defensywnie.
- `build_search_url(criteria, page)`: bazowy URL
  `https://www.otodom.pl/pl/wyniki/sprzedaz/mieszkanie/pomorskie/{slug_miasta}/{slug_miasta}/{slug_miasta}?page={page}` — uprość do działającego wariantu zweryfikowanego w fixturze: `https://www.otodom.pl/pl/wyniki/sprzedaz/mieszkanie/pomorskie/{city}?page={page}`, dołączając `&priceMin/&priceMax/&areaMin/&areaMax/&roomsNumber` gdy podane.
- Na końcu modułu: `register(OtodomScraper())`.

Napisz kod tak, by powyższe testy przeszły; nie zakładaj obecności pól — każdy dostęp z fallbackiem.

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_otodom_parser.py -v`
Expected: PASS (3 testy). Jeśli któreś pole ma inną nazwę niż założono — zbadaj fixture (`load_fixture` + `json`) i dostosuj mapowanie; testy sprawdzają realne dane, więc są źródłem prawdy.

- [ ] **Step 5: Commit**

```bash
git add src/realestate/scrapers/otodom.py tests/scrapers/test_otodom_parser.py
git commit -m "feat: parser otodom.pl (__NEXT_DATA__ → RawListing)"
```

---

### Task 4: Parser nieruchomosci-online.pl (DOM, selectolax)

**Files:**
- Create: `src/realestate/scrapers/nieruchomosci_online.py`
- Test: `tests/scrapers/test_nieruchomosci_online_parser.py`

**Interfaces:**
- Produces: `class NieruchomosciOnlineScraper` (`source_id="nieruchomosci-online"`, `display_name="Nieruchomości-online"`), `build_search_url`/`parse_search`/`parse_detail`; zarejestrowany na końcu modułu.
- Parsowanie DOM przez `selectolax.parser.HTMLParser`. Selektory ustal na podstawie fixture'a `nieruchomosci_online_search_gdansk` (karty ofert, link, cena, metraż, pokoje, lokalizacja).

- [ ] **Step 1: Napisz failujący test**

`tests/scrapers/test_nieruchomosci_online_parser.py`:
```python
from realestate.scrapers.base import SearchCriteria
from realestate.scrapers.nieruchomosci_online import NieruchomosciOnlineScraper
from tests.fixtures.loader import load_fixture


def test_parse_search_returns_listings():
    html = load_fixture("nieruchomosci_online_search_gdansk")
    listings = NieruchomosciOnlineScraper().parse_search(html)
    assert len(listings) >= 10
    first = listings[0]
    assert first.source_id == "nieruchomosci-online"
    assert first.external_id
    assert first.url.startswith("http")
    assert first.title


def test_listings_have_some_prices_and_areas():
    html = load_fixture("nieruchomosci_online_search_gdansk")
    listings = NieruchomosciOnlineScraper().parse_search(html)
    assert any(x.price is not None for x in listings)
    assert any(x.area_m2 is not None for x in listings)


def test_build_search_url_contains_city():
    url = NieruchomosciOnlineScraper().build_search_url(SearchCriteria(city="gdansk"), page=1)
    assert "gda" in url.lower()
```

- [ ] **Step 2: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_nieruchomosci_online_parser.py -v`
Expected: FAIL — brak modułu.

- [ ] **Step 3: Zaimplementuj parser**

Wskazówki:
- Zbadaj fixture: znajdź kontener kart ofert i selektory. Typowo nieruchomosci-online ma karty z linkiem do oferty (`a[href*="/oferta/"]` lub podobne), ceną (element z "zł"), metrażem ("m²"), liczbą pokoi.
- `parse_search(html)`: `HTMLParser(html)`, iteruj po kartach, dla każdej wyłuskaj: `url` (absolutny — dołącz host gdy względny), `external_id` (z URL — ostatni segment/id), `title`, `price` (parsuj liczbę z tekstu "zł", usuń spacje/nbsp), `area_m2` (parsuj "m²"), `rooms`, `district`/`city`. Każde pole defensywnie (None gdy brak).
- Pomocnicze: funkcja `_money(text) -> Decimal | None` (usuń wszystko poza cyframi i separatorem), `_area(text) -> float | None`.
- `parse_detail(html, url)`: wyłuskaj opis i zdjęcia jeśli proste; w razie wątpliwości zwróć minimalny `RawListing`.
- `build_search_url(criteria, page)`: wzór zweryfikowany ręcznie: `https://www.nieruchomosci-online.pl/szukaj.html?3,mieszkanie,sprzedaz,,{Miasto}` + paginacja parametrem strony jeśli dotyczy.
- `register(NieruchomosciOnlineScraper())` na końcu.

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_nieruchomosci_online_parser.py -v`
Expected: PASS (3 testy). Selektory wynikają z realnego fixture'a — dostrój je tak, by testy przeszły.

- [ ] **Step 5: Commit**

```bash
git add src/realestate/scrapers/nieruchomosci_online.py tests/scrapers/test_nieruchomosci_online_parser.py
git commit -m "feat: parser nieruchomosci-online.pl (DOM)"
```

---

### Task 5: Parser hossa.gda.pl (deweloper, DOM)

**Files:**
- Create: `src/realestate/scrapers/hossa.py`
- Test: `tests/scrapers/test_hossa_parser.py`

**Interfaces:**
- Produces: `class HossaScraper` (`source_id="hossa"`, `display_name="Hossa"`), zarejestrowany na końcu modułu. Strona dewelopera — `parse_search` parsuje listę inwestycji/mieszkań ze strony (fixture `hossa_home`). Może zwracać oferty na poziomie inwestycji/mieszkań — wyłuskaj to, co realnie jest w fixturze (linki do inwestycji + ich nazwy/lokalizacje; ceny mogą nie występować — wtedy `price=None`).

- [ ] **Step 1: Napisz failujący test**

`tests/scrapers/test_hossa_parser.py`:
```python
from realestate.scrapers.base import SearchCriteria
from realestate.scrapers.hossa import HossaScraper
from tests.fixtures.loader import load_fixture


def test_parse_search_returns_items():
    html = load_fixture("hossa_home")
    listings = HossaScraper().parse_search(html)
    assert len(listings) >= 1
    first = listings[0]
    assert first.source_id == "hossa"
    assert first.url.startswith("http")
    assert first.title
    assert first.external_id


def test_build_search_url_returns_listing_page():
    url = HossaScraper().build_search_url(SearchCriteria(city="gdansk"), page=1)
    assert url.startswith("https://")
    assert "hossa" in url
```

- [ ] **Step 2: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_hossa_parser.py -v`
Expected: FAIL — brak modułu.

- [ ] **Step 3: Zaimplementuj parser**

Wskazówki:
- Zbadaj fixture `hossa_home`: znajdź sekcję inwestycji/ofert (linki do podstron inwestycji, nazwy, lokalizacje Trójmiasto). Wyłuskaj listę `RawListing` z: `url` (absolutny), `external_id` (slug z URL), `title` (nazwa inwestycji/mieszkania), `district`/`city` jeśli są, `price`/`area_m2` jeśli widoczne (często brak — `None`). `market="primary"` (deweloper = rynek pierwotny).
- `parse_detail(html, url)`: wyłuskaj opis/zdjęcia jeśli proste; inaczej minimalny `RawListing`.
- `build_search_url(criteria, page)`: zwróć URL strony z ofertami dewelopera (np. `https://www.hossa.gda.pl/` lub podstrona listy mieszkań, jeśli wynika z fixture'a).
- `register(HossaScraper())` na końcu.

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_hossa_parser.py -v`
Expected: PASS (2 testy).

- [ ] **Step 5: Commit**

```bash
git add src/realestate/scrapers/hossa.py tests/scrapers/test_hossa_parser.py
git commit -m "feat: parser hossa.gda.pl (deweloper, DOM)"
```

---

### Task 6: Fetcher Playwright (przeglądarka, rate-limit, wykrywanie blokady)

**Files:**
- Create: `src/realestate/scrapers/browser.py`
- Modify: `src/realestate/config.py` (dodaj pola: `scraper_user_agent: str`, `scraper_min_delay_seconds: float = 1.5`, `scraper_nav_timeout_ms: int = 30000`)
- Test: `tests/scrapers/test_browser.py`

**Interfaces:**
- Consumes: `get_settings()`, `ScraperBlocked`.
- Produces:
  - `class BrowserFetcher` (async context manager):
    - `async __aenter__/__aexit__` — startuje/zamyka Playwright + chromium (headless) z konfigurowalnym UA.
    - `async fetch(self, url: str) -> str` — nawiguje, czeka na `networkidle` lub timeout, zwraca `page.content()`; wymusza minimalny odstęp `scraper_min_delay_seconds` między żądaniami; przy wykryciu strony blokady (heurystyka: tytuł/treść zawiera „captcha"/„datadome"/„access denied" i brak oczekiwanej treści) rzuca `ScraperBlocked`.
  - Funkcja `is_blocked(html: str) -> bool` (czysta, testowalna heurystyka).

- [ ] **Step 1: Napisz failujący test (is_blocked czysty + fetch na lokalnym serwerze)**

`tests/scrapers/test_browser.py`:
```python
import functools
import http.server
import threading
from pathlib import Path

import pytest

from realestate.scrapers.browser import BrowserFetcher, is_blocked

FIXT = Path(__file__).parent.parent / "fixtures" / "data"


def test_is_blocked_detects_captcha_page():
    assert is_blocked("<html><title>Captcha</title>Please verify you are a human</html>")
    assert is_blocked("<html>access denied by datadome</html>")


def test_is_blocked_passes_normal_page():
    assert not is_blocked("<html><body><h1>Mieszkania Gdańsk</h1></body></html>")


@pytest.fixture
def static_server(tmp_path):
    # serwuj prosty plik HTML
    (tmp_path / "page.html").write_text("<html><body><h1>OK LISTING</h1></body></html>", encoding="utf-8")
    handler = functools.partial(http.server.SimpleHTTPRequestHandler, directory=str(tmp_path))
    srv = http.server.HTTPServer(("127.0.0.1", 0), handler)
    port = srv.server_address[1]
    t = threading.Thread(target=srv.serve_forever, daemon=True)
    t.start()
    yield f"http://127.0.0.1:{port}"
    srv.shutdown()


@pytest.mark.asyncio
async def test_fetch_returns_html_from_local_server(static_server):
    async with BrowserFetcher() as fetcher:
        html = await fetcher.fetch(f"{static_server}/page.html")
    assert "OK LISTING" in html
```

- [ ] **Step 2: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_browser.py -v`
Expected: FAIL — brak modułu `realestate.scrapers.browser`.

- [ ] **Step 3: Zainstaluj przeglądarkę chromium dla Playwright**

Run: `uv run playwright install chromium`
Expected: pobranie i instalacja chromium (jednorazowo).

- [ ] **Step 4: Zaimplementuj `browser.py` + pola konfiguracji**

`config.py` — dodaj do `Settings`:
```python
    scraper_user_agent: str = (
        "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 "
        "(KHTML, like Gecko) Chrome/124.0 Safari/537.36"
    )
    scraper_min_delay_seconds: float = 1.5
    scraper_nav_timeout_ms: int = 30000
```

`src/realestate/scrapers/browser.py`:
```python
from __future__ import annotations

import asyncio
import time

from playwright.async_api import async_playwright

from realestate.config import get_settings
from realestate.scrapers.base import ScraperBlocked

_BLOCK_MARKERS = ("captcha", "datadome", "access denied", "verify you are a human")


def is_blocked(html: str) -> bool:
    low = html.lower()
    # heurystyka: marker blokady + brak typowej treści ofert
    if any(m in low for m in _BLOCK_MARKERS) and "listing" not in low and "oferta" not in low:
        return True
    return False


class BrowserFetcher:
    def __init__(self) -> None:
        self._settings = get_settings()
        self._pw = None
        self._browser = None
        self._context = None
        self._last_fetch = 0.0

    async def __aenter__(self) -> "BrowserFetcher":
        self._pw = await async_playwright().start()
        self._browser = await self._pw.chromium.launch(headless=True)
        self._context = await self._browser.new_context(
            user_agent=self._settings.scraper_user_agent
        )
        return self

    async def __aexit__(self, *exc) -> None:
        if self._context:
            await self._context.close()
        if self._browser:
            await self._browser.close()
        if self._pw:
            await self._pw.stop()

    async def _throttle(self) -> None:
        delay = self._settings.scraper_min_delay_seconds
        elapsed = time.monotonic() - self._last_fetch
        if elapsed < delay:
            await asyncio.sleep(delay - elapsed)
        self._last_fetch = time.monotonic()

    async def fetch(self, url: str) -> str:
        await self._throttle()
        page = await self._context.new_page()
        try:
            await page.goto(
                url, wait_until="domcontentloaded",
                timeout=self._settings.scraper_nav_timeout_ms,
            )
            html = await page.content()
        finally:
            await page.close()
        if is_blocked(html):
            raise ScraperBlocked(url)
        return html
```

(Uwaga: dla `get_settings()` w teście wymagany jest `DATABASE_URL` w env — globalny fixture `_clear_settings_cache` to obsługuje, ale `Settings` wymaga `database_url`. Jeśli test fetchera nie ma env, ustaw w teście `monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@localhost/db")` przed użyciem `BrowserFetcher`, albo dodaj autouse fixture w `tests/scrapers/conftest.py` ustawiający DATABASE_URL. Implementer: dodaj `tests/scrapers/conftest.py` z autouse fixture ustawiającym `DATABASE_URL`, by `get_settings()` działało bez prawdziwej bazy.)

- [ ] **Step 5: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_browser.py -v`
Expected: PASS (3 testy; `test_fetch_returns_html_from_local_server` realnie uruchamia chromium na lokalnym serwerze).

- [ ] **Step 6: Commit**

```bash
git add src/realestate/scrapers/browser.py src/realestate/config.py tests/scrapers/
git commit -m "feat: fetcher Playwright (rate-limit, wykrywanie blokady) + konfiguracja scrapera"
```

---

### Task 7: Orkiestracja wyszukiwania (search po stronach) + test integracyjny na lokalnym serwerze

**Files:**
- Create: `src/realestate/scrapers/runner.py`
- Test: `tests/scrapers/test_runner.py`

**Interfaces:**
- Consumes: `Scraper`, `BrowserFetcher` (lub dowolny obiekt z `async fetch(url)->str`), `SearchCriteria`, `RawListing`.
- Produces:
  - `async def run_search(scraper: Scraper, fetcher, criteria: SearchCriteria, *, max_pages: int = 1) -> list[RawListing]` — dla stron `1..max_pages` buduje URL (`scraper.build_search_url`), pobiera (`fetcher.fetch`), parsuje (`scraper.parse_search`), zbiera wyniki; deduplikuje po `(source_id, external_id)`; przerywa wcześnie gdy strona zwróci 0 ofert. `ScraperBlocked` z fetchera propaguje (orkiestracja go nie połyka — wyższa warstwa/ingestia zdecyduje).

- [ ] **Step 1: Napisz failujący test (z atrapą fetchera + realnym parserem na fixturze)**

`tests/scrapers/test_runner.py`:
```python
import pytest

from realestate.scrapers.base import ScraperBlocked, SearchCriteria
from realestate.scrapers.otodom import OtodomScraper
from realestate.scrapers.runner import run_search
from tests.fixtures.loader import load_fixture


class _FakeFetcher:
    def __init__(self, pages: dict[str, str]):
        self.pages = pages
        self.calls = []

    async def fetch(self, url: str) -> str:
        self.calls.append(url)
        # zwróć fixture dla strony 1, pustą listę-stronę dla kolejnych
        return self.pages.get(url, "<html><script id=\"__NEXT_DATA__\" type=\"application/json\">{\"props\":{\"pageProps\":{\"data\":{\"searchAds\":{\"items\":[]}}}}}</script></html>")


@pytest.mark.asyncio
async def test_run_search_collects_and_dedups():
    scraper = OtodomScraper()
    url1 = scraper.build_search_url(SearchCriteria(city="gdansk"), 1)
    fetcher = _FakeFetcher({url1: load_fixture("otodom_search_gdansk")})
    listings = await run_search(scraper, fetcher, SearchCriteria(city="gdansk"), max_pages=3)
    assert len(listings) >= 20
    ids = [(x.source_id, x.external_id) for x in listings]
    assert len(ids) == len(set(ids))  # bez duplikatów


@pytest.mark.asyncio
async def test_run_search_stops_on_empty_page():
    scraper = OtodomScraper()
    fetcher = _FakeFetcher({})  # każda strona pusta
    listings = await run_search(scraper, fetcher, SearchCriteria(city="gdansk"), max_pages=5)
    assert listings == []
    # przerwał po pierwszej pustej stronie
    assert len(fetcher.calls) == 1


class _BlockingFetcher:
    async def fetch(self, url: str) -> str:
        raise ScraperBlocked(url)


@pytest.mark.asyncio
async def test_run_search_propagates_blocked():
    with pytest.raises(ScraperBlocked):
        await run_search(OtodomScraper(), _BlockingFetcher(), SearchCriteria(city="gdansk"), max_pages=1)
```

- [ ] **Step 2: Uruchom test — ma faliować**

Run: `uv run pytest tests/scrapers/test_runner.py -v`
Expected: FAIL — brak modułu `realestate.scrapers.runner`.

- [ ] **Step 3: Zaimplementuj `runner.py`**

`src/realestate/scrapers/runner.py`:
```python
from __future__ import annotations

from realestate.scrapers.base import RawListing, Scraper, SearchCriteria


async def run_search(
    scraper: Scraper,
    fetcher,
    criteria: SearchCriteria,
    *,
    max_pages: int = 1,
) -> list[RawListing]:
    seen: set[tuple[str, str]] = set()
    results: list[RawListing] = []
    for page in range(1, max_pages + 1):
        url = scraper.build_search_url(criteria, page)
        html = await fetcher.fetch(url)
        page_listings = scraper.parse_search(html)
        if not page_listings:
            break
        for listing in page_listings:
            key = (listing.source_id, listing.external_id)
            if key in seen:
                continue
            seen.add(key)
            results.append(listing)
    return results
```

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `uv run pytest tests/scrapers/test_runner.py -v`
Expected: PASS (3 testy).

- [ ] **Step 5: Pełny zestaw + lint**

Run: `uv run pytest && uv run ruff check .`
Expected: wszystkie testy PASS; ruff czysto.

- [ ] **Step 6: Commit**

```bash
git add src/realestate/scrapers/runner.py tests/scrapers/test_runner.py
git commit -m "feat: orkiestracja run_search (paginacja, dedup, propagacja blokady)"
```

---

## Definicja ukończenia (Plan 2)
- `uv run pytest` zielony; `uv run ruff check .` bez błędów.
- Trzy zarejestrowane wtyczki (`otodom`, `nieruchomosci-online`, `hossa`) z działającymi parserami na realnych fixture'ach.
- Fetcher Playwright pobiera HTML (zweryfikowane na lokalnym serwerze, chromium), z rate-limitem i wykrywaniem blokady.
- `run_search` zbiera i deduplikuje oferty po stronach.
- Opcjonalne testy „na żywo" (`@pytest.mark.live`) — poza domyślnym przebiegiem.
