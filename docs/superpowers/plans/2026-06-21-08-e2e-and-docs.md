# E2E + dokumentacja techniczna — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Domknąć produkt: (1) podpiąć scheduler do cyklu życia aplikacji (lifespan) tak, by `scheduler_enabled`/`scheduler_default_interval_minutes` faktycznie działały, ze smoke-testem startu; (2) dodać test E2E przechodzący przez całe API (scrape → lista → szczegóły → ulubione); (3) napisać kompletną dokumentację techniczną (architektura, uruchomienie, konfiguracja, dodawanie wtyczki scrapera, testy) + README.

**Architecture:** `create_app()` lifespan buduje silnik DB i `EventBus` (istnieje); ten plan dodaje warunkowy start `ScrapeScheduler` (gdy `settings.scheduler_enabled`) z interwałem `settings.scheduler_default_interval_minutes`, zatrzymywany przy zamknięciu. Test E2E używa realnego kontenera pg18 + fake fetcher + degradacji LLM (bez klienta) i przechodzi pełną ścieżkę przez `httpx.ASGITransport`. Dokumentacja w `docs/` + `README.md`.

**Tech Stack:** Python 3.14, FastAPI (lifespan przez `app.router.lifespan_context`), APScheduler, SQLAlchemy async, httpx (ASGITransport), pytest. Markdown do dokumentacji.

## Global Constraints

- Stack: Python 3.14, SQLAlchemy 2.0 async + asyncpg, PostgreSQL 18 + pgvector, FastAPI, APScheduler. Uruchamianie: `uv run`.
- TDD dla kodu (Task 1-2): test → implementacja → commit. Testy na realnym kontenerze pg18 (`engine`/`pg_url` z `tests/conftest.py`).
- Lint `uv run ruff check .` musi przechodzić (E,F,I,UP,B; line-length 100; `# noqa: B008` dozwolone na `Depends()`).
- Lifespan: NIE wolno wymagać `DATABASE_URL` przy konstrukcji `create_app()` (tylko w lifespan startup) — istniejące testy API (ASGITransport bez lifespanu) muszą dalej działać.
- Auto-start schedulera tylko gdy `settings.scheduler_enabled` jest prawdziwe; przy zamknięciu lifespanu scheduler musi zostać zatrzymany (brak wiszących wątków).
- Brak zmian schematu (żadnej migracji). Brak nowych zależności runtime poza już obecnymi.
- Dokumentacja: polecenia w dokumentacji MUSZĄ być zgodne z rzeczywistością repo (zweryfikowane). Brak sekretów w dokumentacji (tylko `.env.example`-owe wzorce).
- Pyright/import-resolution błędy to znane false-positives (src-layout) — brama jakości to `ruff` + `pytest` (oraz, dla frontu, jego własna brama — nietykana w tym planie).

---

### Task 1: Auto-start schedulera w lifespan aplikacji + smoke-test startu

**Files:**
- Modify: `src/realestate/api/app.py`
- Test: `tests/api/test_lifespan.py`

**Interfaces:**
- Consumes: `get_settings`, `create_engine`, `create_session_factory`, `EventBus`, `ScrapeScheduler` (z `realestate.scheduler.runner`), `BrowserFetcher`.
- Produces (zmiana w `create_app()` lifespan):
  - startup (jak dotąd): `app.state.engine`, `app.state.session_factory`, `app.state.event_bus`.
  - NOWE: `app.state.scheduler = None` domyślnie; jeśli `settings.scheduler_enabled` → utwórz `ScrapeScheduler(app.state.session_factory, BrowserFetcher(), app.state.event_bus)`, `scheduler.start(interval_minutes=settings.scheduler_default_interval_minutes)`, zapisz na `app.state.scheduler`.
  - shutdown: jeśli `app.state.scheduler is not None` → `app.state.scheduler.shutdown()`; następnie `await engine.dispose()`.
  - `BrowserFetcher` importowany LENIWIE wewnątrz lifespanu (nie na poziomie modułu — by import `app` nie ciągnął playwright).

- [ ] **Step 1: Write the failing test**

```python
# tests/api/test_lifespan.py
import pytest

from realestate.api.app import create_app


async def test_lifespan_sets_state_without_scheduler_by_default(pg_url, monkeypatch):
    monkeypatch.setenv("DATABASE_URL", pg_url)
    monkeypatch.delenv("SCHEDULER_ENABLED", raising=False)
    app = create_app()
    async with app.router.lifespan_context(app):
        assert app.state.engine is not None
        assert app.state.session_factory is not None
        assert app.state.event_bus is not None
        assert app.state.scheduler is None  # domyślnie wyłączony


async def test_lifespan_starts_scheduler_when_enabled(pg_url, monkeypatch):
    monkeypatch.setenv("DATABASE_URL", pg_url)
    monkeypatch.setenv("SCHEDULER_ENABLED", "true")
    monkeypatch.setenv("SCHEDULER_DEFAULT_INTERVAL_MINUTES", "999")
    app = create_app()
    async with app.router.lifespan_context(app):
        assert app.state.scheduler is not None
        jobs = app.state.scheduler.jobs()
        assert len(jobs) == 1
        assert jobs[0].id == "scrape"
    # po wyjściu z lifespanu scheduler jest zatrzymany
    assert app.state.scheduler._scheduler.running is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/api/test_lifespan.py -v`
Expected: FAIL — brak `app.state.scheduler` / scheduler nie startuje.

- [ ] **Step 3: Write minimal implementation**

W `src/realestate/api/app.py` zmodyfikuj funkcję `lifespan` wewnątrz `create_app()`. Dodaj importy na górze pliku: `from realestate.events.bus import EventBus` (jeśli już jest — zostaw) oraz `from realestate.scheduler.runner import ScrapeScheduler`. `BrowserFetcher` importuj leniwie w lifespanie. Przykład docelowego lifespanu:

```python
    @asynccontextmanager
    async def lifespan(app: FastAPI):
        settings = get_settings()
        engine = create_engine(settings.database_url)
        app.state.engine = engine
        app.state.session_factory = create_session_factory(engine)
        app.state.event_bus = EventBus()
        app.state.scheduler = None
        if settings.scheduler_enabled:
            from realestate.scrapers.browser import BrowserFetcher

            scheduler = ScrapeScheduler(
                app.state.session_factory, BrowserFetcher(), app.state.event_bus
            )
            scheduler.start(interval_minutes=settings.scheduler_default_interval_minutes)
            app.state.scheduler = scheduler
        try:
            yield
        finally:
            if app.state.scheduler is not None:
                app.state.scheduler.shutdown()
            await engine.dispose()
```

(Zachowaj istniejące `app.include_router(...)` i `/health` bez zmian.)

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/api/test_lifespan.py -v`
Expected: PASS (2 testy).

- [ ] **Step 5: Commit**

```bash
git add src/realestate/api/app.py tests/api/test_lifespan.py
git commit -m "feat: auto-start schedulera w lifespan (gdy scheduler_enabled) + smoke-test"
```

---

### Task 2: Test E2E pełnej ścieżki API (scrape → lista → szczegóły → ulubione)

**Files:**
- Test: `tests/e2e/__init__.py` (pusty), `tests/e2e/test_full_flow.py`

**Interfaces:**
- Consumes: `create_app`, zależności DI (`get_session`, `get_session_factory`, `get_fetcher_dep`, `get_event_bus_dep`, `get_llm_client_dep`), `create_session_factory`, `EventBus`, fixture `engine`, realny parser otodom + fixture HTML.
- Produces: jeden spójny test E2E przez `httpx.ASGITransport`, weryfikujący integrację warstw przez HTTP (bez sieci — fake fetcher; LLM wyłączony → degradacja regułowa).

- [ ] **Step 1: Write the failing test**

```python
# tests/e2e/test_full_flow.py
import pytest
from httpx import ASGITransport, AsyncClient

from realestate.api.app import create_app
from realestate.api.deps import (
    get_event_bus_dep,
    get_fetcher_dep,
    get_llm_client_dep,
    get_session,
    get_session_factory,
)
from realestate.db.engine import create_session_factory
from realestate.events.bus import EventBus
from realestate.models import Base
from tests.fixtures.loader import load_fixture


class _OneSourceFetcher:
    def __init__(self):
        self.first = True
    async def fetch(self, url: str) -> str:
        if self.first:
            self.first = False
            return load_fixture("otodom_search_gdansk")
        return '<html><script id="__NEXT_DATA__" type="application/json">{"props":{"pageProps":{"data":{"searchAds":{"items":[]}}}}}</script></html>'


@pytest.fixture(autouse=True)
def _only_otodom():
    import realestate.scrapers.base as base
    import realestate.scrapers.otodom  # noqa: F401
    saved = dict(base._REGISTRY)
    base._REGISTRY.clear()
    base._REGISTRY.update({"otodom": saved["otodom"]})
    yield
    base._REGISTRY.clear()
    base._REGISTRY.update(saved)


def _app(engine):
    app = create_app()
    factory = create_session_factory(engine)

    async def _session():
        async with factory() as s:
            yield s

    app.dependency_overrides[get_session] = _session
    app.dependency_overrides[get_session_factory] = lambda: factory
    app.dependency_overrides[get_fetcher_dep] = lambda: _OneSourceFetcher()
    app.dependency_overrides[get_event_bus_dep] = lambda: EventBus()
    app.dependency_overrides[get_llm_client_dep] = lambda: None  # degradacja regułowa
    return app


async def test_full_flow_scrape_list_detail_favorite(engine):
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    app = _app(engine)
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://t") as client:
        # 1) scrape
        scraped = await client.post("/scrape", json={"city": "gdansk",
                                                     "source_ids": ["otodom"], "max_pages": 2})
        assert scraped.status_code == 200
        assert scraped.json()["runs"][0]["status"] == "success"
        assert scraped.json()["runs"][0]["new_count"] >= 20

        # 2) lista (ranking regułowy, bez LLM)
        listed = await client.get("/listings", params={"limit": 100})
        body = listed.json()
        assert body["total"] >= 20
        first_id = body["items"][0]["id"]

        # 3) szczegóły
        detail = await client.get(f"/listings/{first_id}")
        assert detail.status_code == 200
        assert detail.json()["id"] == first_id

        # 4) ulubione
        fav = await client.post("/favorites", json={"listing_id": first_id})
        assert fav.status_code == 201
        favs = await client.get("/favorites")
        assert any(f["listing_id"] == first_id for f in favs.json())
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/e2e/test_full_flow.py -v`
Expected: na początku FAIL jeśli czegokolwiek brakuje; docelowo po implementacji (nic nowego w kodzie — to test integracyjny istniejących endpointów) powinien przejść. Jeśli przechodzi od razu — to potwierdza integrację; mimo to dodaj `tests/e2e/__init__.py`.

- [ ] **Step 3: Implementacja**

Brak nowego kodu produkcyjnego — to test E2E istniejącego API. Utwórz `tests/e2e/__init__.py` (pusty). Jeśli test ujawni realny błąd integracji, napraw minimalnie i odnotuj.

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/e2e/test_full_flow.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/__init__.py tests/e2e/test_full_flow.py
git commit -m "test: E2E pełnej ścieżki API (scrape -> lista -> szczegóły -> ulubione)"
```

---

### Task 3: Dokumentacja techniczna + README

**Files:**
- Create: `README.md`
- Create: `docs/architecture.md`
- Create: `docs/running.md`
- Create: `docs/configuration.md`
- Create: `docs/adding-a-scraper.md`
- Create: `docs/testing.md`
- Modify: `AGENTS.md` (dodaj sekcję o froncie + skrót dokumentacji)
- Test: `tests/test_docs_present.py`

**Interfaces:**
- Produces: kompletna dokumentacja. Lekki test sprawdzający obecność kluczowych plików i sekcji (by dokumentacja nie „zniknęła").

- [ ] **Step 1: Write the failing test**

```python
# tests/test_docs_present.py
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parent.parent

REQUIRED = {
    "README.md": ["Architektura", "Uruchomienie", "Konfiguracja"],
    "docs/architecture.md": ["Warstwy", "pgvector", "wyszukiwani"],
    "docs/running.md": ["docker compose", "uv run", "uvicorn", "frontend"],
    "docs/configuration.md": ["EMBEDDING_DIM", "LLM_", "scheduler"],
    "docs/adding-a-scraper.md": ["Scraper", "register", "RawListing"],
    "docs/testing.md": ["pytest", "testcontainers", "vitest"],
}


@pytest.mark.parametrize("path,needles", REQUIRED.items())
def test_doc_exists_and_mentions(path, needles):
    file = ROOT / path
    assert file.exists(), f"brak pliku dokumentacji: {path}"
    text = file.read_text(encoding="utf-8")
    for needle in needles:
        assert needle in text, f"{path} nie zawiera oczekiwanego fragmentu: {needle!r}"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_docs_present.py -v`
Expected: FAIL — brak plików dokumentacji.

- [ ] **Step 3: Napisz dokumentację**

Utwórz pliki o treści zgodnej z rzeczywistym repo. Wymagana zawartość (każdy plik rzetelny, zweryfikowany względem kodu):

**`README.md`** — sekcje: krótki opis (agregator ofert mieszkań w Trójmieście: scraping Playwright → PostgreSQL+pgvector → wzbogacanie/wyszukiwanie LLM → API + SPA), **Architektura** (lista warstw + odnośnik do `docs/architecture.md`), **Uruchomienie** (skrót: `docker compose up -d db`; `uv run alembic upgrade head` z `EMBEDDING_DIM`; `uv run uvicorn realestate.api.app:app --reload`; front: `cd frontend && npm install && npm run dev`; odnośnik do `docs/running.md`), **Konfiguracja** (odnośnik do `docs/configuration.md`, wzmianka o `.env`/`.env.example`, że klucz LLM nigdy nie jest zwracany przez API), **Testy** (`uv run pytest`, `uv run ruff check .`, front: `cd frontend && npm test -- --run`), **Dokumentacja** (lista plików w `docs/`).

**`docs/architecture.md`** — opis warstw (Scrapery (Playwright, rejestr wtyczek, `Scraper` Protocol) → Normalizacja/`raw_hash` → Magazyn PostgreSQL+pgvector (modele: Source, Listing, PriceHistory, ScrapeRun, LLMAnalysis, DedupGroup/Member, SavedSearch/Favorite/AppSetting) → Silnik inkrementalny (`IncrementalEngine`) → Warstwa LLM (`LLMClient`, OpenAICompat/Fake, EnrichmentService, DedupService) → Wyszukiwanie hybrydowe (`SearchService`: filtry SQL → pgvector top-K → rerank LLM, degradacja) → API (FastAPI) + SSE (EventBus) + Scheduler (APScheduler) → Frontend (React/Vite/TS)). Musi zawierać słowa „Warstwy", „pgvector", „wyszukiwani". Diagram tekstowy przepływu danych mile widziany. Wspomnij o jednym źródle wymiaru embeddingu `get_embedding_dim()`.

**`docs/running.md`** — pełna instrukcja: wymagania (Python 3.14, uv, Node 26+, Docker), `docker compose up -d db` (obraz `pgvector/pgvector:pg18`), zmienne env (`DATABASE_URL`, `EMBEDDING_DIM`), migracje `EMBEDDING_DIM=1536 uv run alembic upgrade head` (z uwagą, że Alembic nie ładuje `.env`), uruchomienie API `uv run uvicorn realestate.api.app:app --reload`, ręczny scrape przez `POST /scrape`, podgląd postępu `GET /events`, uruchomienie frontu (`cd frontend && npm install && npm run dev`, zmienna `VITE_API_BASE`). Musi zawierać „docker compose", „uv run", „uvicorn", „frontend".

**`docs/configuration.md`** — wszystkie ustawienia `pydantic-settings` (`Settings` w `realestate/config.py`): `DATABASE_URL`, `EMBEDDING_DIM` (jedno źródło, wymagane też przy `alembic upgrade`), scraper (`SCRAPER_*`), LLM (`LLM_BASE_URL` domyślnie OpenRouter, `LLM_API_KEY`, `LLM_MODEL`, `LLM_EMBEDDING_MODEL`, `LLM_TIMEOUT_SECONDS`, `LLM_MAX_RETRIES`; bez modelu/klucza → LLM wyłączony, degradacja), scheduler (`SCHEDULER_ENABLED`, `SCHEDULER_DEFAULT_INTERVAL_MINUTES`). Zaznacz, że sekrety idą do `.env` (wzór: `.env.example`) i `GET /settings` NIGDY nie zwraca `LLM_API_KEY` (tylko `llm_api_key_set`). Musi zawierać „EMBEDDING_DIM", „LLM_", „scheduler".

**`docs/adding-a-scraper.md`** — jak dodać nową wtyczkę: zaimplementuj `Scraper` Protocol (`source_id`, `display_name`, `build_search_url`, `parse_search`, `parse_detail`), zwracaj `RawListing`, zarejestruj przez `register(...)` w module, dodaj fixture HTML w `tests/fixtures/data/` i test offline. Wspomnij o `docs/scrapers-field-contract.md`. Musi zawierać „Scraper", „register", „RawListing".

**`docs/testing.md`** — strategia testów: backend `uv run pytest` (pytest-asyncio, testcontainers pg18+pgvector — wymaga Dockera), markery (`live`), lint `uv run ruff check .`; front `cd frontend && npm test -- --run` (vitest + Testing Library + MSW) i `npm run build`. Wspomnij, że Pyright src-layout import-errors to znane false-positives. Musi zawierać „pytest", „testcontainers", „vitest".

**`AGENTS.md`** — dopisz sekcję „## Frontend" (katalog `frontend/`, `npm install`, `npm run dev`, `npm run build`, `npm test -- --run`, stack: React/Vite/TS, `VITE_API_BASE`) oraz „## Dokumentacja" wskazującą `docs/` i `README.md`. Nie zmieniaj istniejących sekcji poza dodaniem nowych.

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_docs_present.py -v`
Expected: PASS (6 sparametryzowanych przypadków).

- [ ] **Step 5: Pełny zestaw + lint + weryfikacja poleceń**

Run: `uv run pytest && uv run ruff check .`
Expected: cały backend zielony, ruff czysty.
Dodatkowo zweryfikuj ręcznie, że polecenia z dokumentacji są zgodne z repo (np. ścieżka `realestate.api.app:app`, komenda alembic z `EMBEDDING_DIM`, polecenia npm w `frontend/package.json`).

- [ ] **Step 6: Commit**

```bash
git add README.md docs/ AGENTS.md tests/test_docs_present.py
git commit -m "docs: dokumentacja techniczna (architektura, uruchomienie, konfiguracja, wtyczki, testy) + README"
```

---

## Definicja ukończenia (Plan 8)
- `uv run pytest` zielony; `uv run ruff check .` bez błędów.
- Scheduler startuje w lifespanie gdy `SCHEDULER_ENABLED` i jest zatrzymywany przy zamknięciu (smoke-test); `scheduler_enabled`/`scheduler_default_interval_minutes` nie są już „osierocone".
- Test E2E przechodzi pełną ścieżkę przez HTTP (scrape → lista → szczegóły → ulubione) na realnej bazie pg18, z degradacją LLM.
- Kompletna dokumentacja: `README.md` + `docs/{architecture,running,configuration,adding-a-scraper,testing}.md`, zgodna z repo; `AGENTS.md` uzupełniony o front i dokumentację; lekki test obecności dokumentacji.
